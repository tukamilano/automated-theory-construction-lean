---
name: lean-rule
description: Rules to apply this repo's Lean workflow (plan -> skeleton -> error-driven iteration -> mathlib search -> minimal diffs) consistently across all `.lean` files.
---

# Compatibility shim

This file is a compatibility shim for tools that still read skills from `.codex/`.

The source of truth is:

- `.agents/shared/skills/lean-rule/SKILL.md`

Related shared workflow guidance:

- `.agents/shared/workflow/proof-planning.md`

Apply the same rules as the shared file above. If this file and the shared file ever differ, follow the shared file.
