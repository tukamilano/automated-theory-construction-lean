---
name: mathlib-usage
description: Mathlib usage principles (imports, search, existence checks, confirmation) for all `.lean` files in this repo.
---

# Compatibility shim

This file is a compatibility shim for tools that still read skills from `.codex/`.

The source of truth is:

- `.agents/shared/skills/mathlib-usage/SKILL.md`

Apply the same rules as the shared file above. If this file and the shared file ever differ, follow the shared file.
