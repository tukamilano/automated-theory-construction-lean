# Problem op_000006

## Statement
∀ {α : Type u} [SemigroupLike01 α], ∀ x y z : α, x * y = x * z → y = z

## Attempt Result
counterexample

## Natural Language Sketch
`α := ULift (Fin 3)` とし、`zero = 0`, `one = 1`, `top = 2` を取る。演算を `x * y := if x = y then x else top` と定めると、`SemigroupLike01` の 3 公理は有限計算で成り立つ。ところが `top * zero = top * one` である一方 `zero ≠ one` なので、左可約性 `x * y = x * z → y = z` はこのモデルで壊れる。

## Counterexample Intuition
`ULift (Fin 3)` 上で、2 つが等しければその値、異なれば常に `top` を返す 3 元演算を考える。この演算は与えられた 3 公理を満たすが、`top * zero` と `top * one` はどちらも `top` になり、しかも `zero` と `one` は異なる。したがって左可約性は一般には成り立たない。

## Lean Draft
```lean
intro h
let α : Type u := ULift.{u} (Fin 3)
let zero : α := ⟨(0 : Fin 3)⟩
let one : α := ⟨(1 : Fin 3)⟩
let top : α := ⟨(2 : Fin 3)⟩
let op3 : α → α → α := fun x y => if x = y then x else top
letI : Mul α := ⟨op3⟩
letI : SemigroupLike01 α :=
  { mul := op3
    ax_left_idempotent := by
      intro x
      change op3 x x = x
      simp [op3]
    ax_right_absorb_duplicate := by
      native_decide
    ax_middle_swap := by
      native_decide }
have hEq : top * zero = top * one := by
  change op3 top zero = op3 top one
  simp [op3, top, zero, one]
have hContra : zero = one := h (α := α) top zero one hEq
have hne : zero ≠ one := by
  decide
exact hne hContra
```
