# Problem op_000007

## Statement
∀ {α : Type u} [SemigroupLike01 α], ∀ x : α, x * x = x

## Attempt Result
proof

## Natural Language Sketch
α, インスタンス, x を導入する。クラス公理 `SemigroupLike01.ax_left_idempotent` から `op x x = x` を得るので、`op` を展開して `x * x = x` に `simpa` で直す。

## Counterexample Intuition
(empty)

## Lean Draft
```lean
intro α _ x
simpa [op] using SemigroupLike01.ax_left_idempotent (α := α) x
```
